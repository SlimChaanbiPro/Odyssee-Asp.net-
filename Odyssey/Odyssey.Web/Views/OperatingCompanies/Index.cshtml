@model IEnumerable<Odyssey.Domain.Entities.OperatingCompany>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OperatingCompanyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OperatingCompanyDetails)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OperatingCompanyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OperatingCompanyDetails)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OperatingCompanyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OperatingCompanyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OperatingCompanyId })
        </td>
    </tr>
}

</table>
